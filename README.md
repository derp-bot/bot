# bot

The actual bot itself.

## Dev

This bot requires a Discord bot token to start; it literally will throw an error and exit if not provided via an environment variable. Once you have one, set it as `DERP_BOT_TOKEN` in your environment and you're good to go.

Check out [the config module](src/config.js) for other, useful environment variables.
